# Configurações de banco de dados MySQL usando variáveis de ambiente
spring.datasource.url=jdbc:mysql://localhost:3306/db_flor_da_cidade?useSSL=false&serverTimezone=UTC
spring.datasource.username=Fernando_Correia
spring.datasource.password=060163
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update  # Cria ou atualiza o esquema de acordo com as entidades
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuração do Flyway
flyway.baseline-on-migrate=true
flyway.enabled=true
